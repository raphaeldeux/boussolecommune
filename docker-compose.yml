services:
  web:
    build: .
    restart: always
    ports:
      - "127.0.0.1:5001:5000"   # Exposé uniquement en local — Nginx fait le reverse proxy
    volumes:
      - ./data:/app/data
      - ./uploads:/app/uploads
    env_file: .env
